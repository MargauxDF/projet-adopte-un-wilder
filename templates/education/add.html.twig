{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-education w-50 mx-auto">
        <h2 class="text-center mt-5">Ajouter une formation</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary" type="submit">Sauvegarder</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
