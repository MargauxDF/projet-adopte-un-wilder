{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-skill w-50 mx-auto">
        <h2 class="text-center mt-5">Modifier Vos Compétences</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary" type="submit">Sauvegarder</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
